/**
 * Função de teste mantida para compatibilidade
 * @param params - Objeto com valor string
 * @returns O valor passado como parâmetro
 */
export function teste({ value }: { value: string }): string {
  return value
}
